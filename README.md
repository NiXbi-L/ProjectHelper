# ProjectHelper

Веб-приложение для управления проектной деятельностью студентов с канбан-доской, системой этапов и проверкой преподавателями.

## 🚀 Возможности

- **Авторизация через Яндекс OAuth** (только для @dvfu.ru)
- **Управление командами и проектами**
- **Канбан-доска** с перетаскиванием карточек между колонками
- **Этапы проекта** с комментариями и файлами
- **Паспорт проекта** (загрузка файлом)
- **Панель преподавателя** для проверки всех проектов
- **Система комментариев и проверок**

## 📋 Технологии

- **Backend**: Django REST Framework, PostgreSQL
- **Frontend**: React.js, Material-UI, @dnd-kit (drag-and-drop)
- **Инфраструктура**: Docker, Docker Compose, Nginx

## 🛠 Установка и запуск

### Требования

- Docker
- Docker Compose

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd ProjectHelper
```

2. **Создайте файл `.env` в корне проекта:**
```env
SECRET_KEY=your-secret-key-here
YANDEX_OAUTH2_KEY=your-yandex-oauth2-client-id
YANDEX_OAUTH2_SECRET=your-yandex-oauth2-client-secret
YANDEX_OAUTH2_REDIRECT_URI=http://localhost:8080/auth/complete/yandex-oauth2/
TEACHER_REGISTRATION_KEY=your-teacher-registration-key
DB_NAME=projecthelper
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
```

3. **Запустите проект:**
```bash
docker-compose up --build
```

4. **Откройте в браузере:**
```
http://localhost:8080
```

## 🔐 Настройка Яндекс OAuth

1. Перейдите на [https://oauth.yandex.ru/](https://oauth.yandex.ru/)
2. Создайте новое приложение:
   - **Название**: ProjectHelper
   - **Платформы**: Web-сервисы
   - **Callback URI**: `http://localhost:8080/auth/complete/yandex-oauth2/`
3. Скопируйте **Client ID** и **Client Secret**
4. Добавьте их в файл `.env`:
   ```env
   YANDEX_OAUTH2_KEY=ваш-client-id
   YANDEX_OAUTH2_SECRET=ваш-client-secret
   ```

**Важно:** Callback URI должен точно совпадать с указанным в Яндекс OAuth!

## 👥 Регистрация преподавателя

Преподаватели могут зарегистрироваться, используя специальный ключ регистрации:

1. Перейдите на страницу регистрации
2. Введите ключ регистрации (указан в `.env` как `TEACHER_REGISTRATION_KEY`)
3. Заполните форму регистрации

Преподаватели имеют доступ ко всем проектам в системе и могут:
- Просматривать канбан-доски всех проектов
- Ставить галочки проверки
- Оставлять комментарии
- Отправлять проекты на доработку

## 📁 Структура проекта

```
ProjectHelper/
├── API/                    # Django Backend
│   ├── config/            # Настройки Django
│   ├── projects/          # Приложение проектов
│   │   ├── models.py      # Модели (Project, Stage, KanbanCard и др.)
│   │   ├── views.py       # API ViewSets
│   │   ├── serializers.py # Сериализаторы
│   │   └── permissions.py # Права доступа
│   ├── users/             # Приложение пользователей
│   ├── manage.py
│   └── requirements.txt
├── frontend/              # React Frontend
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   │   ├── ProjectDetail.js    # Детали проекта с канбан-доской
│   │   │   ├── ProjectKanban.js    # Канбан-доска проекта
│   │   │   ├── ProjectsList.js     # Список проектов
│   │   │   └── TeacherDashboard.js # Панель преподавателя
│   │   └── context/       # React Context (AuthContext)
│   └── package.json
├── nginx/                 # Конфигурация Nginx
├── docker-compose.yml     # Docker Compose конфигурация
└── README.md
```

## 🎯 Основные функции

### Канбан-доска

- **3 колонки**: "В работе", "На проверке", "Завершено"
- **Перетаскивание карточек** между колонками (drag-and-drop)
- **Создание карточек** всеми участниками команды
- **Прикрепление файлов** к карточкам
- **Комментарии** к карточкам
- **Проверки преподавателя** с галочками

### Паспорт проекта

- Загрузка паспорта проекта **файлом** (PDF, DOC, DOCX, TXT)
- Опциональный текстовый вариант
- Доступен через модальное окно в интерфейсе проекта

### Этапы проекта

- Создаются после принятия паспорта проекта преподавателем
- Каждый этап может содержать:
  - Описание
  - Файлы (артефакты)
  - Комментарии
  - Статус (в работе, на проверке, принято, на доработке)

## 🐳 Docker команды

### Запуск в фоновом режиме
```bash
docker-compose up -d
```

### Остановка
```bash
docker-compose down
```

### Просмотр логов
```bash
docker-compose logs -f
```

### Выполнение команд в контейнере API
```bash
# Создание суперпользователя
docker-compose exec api python manage.py createsuperuser

# Применение миграций
docker-compose exec api python manage.py migrate

# Создание миграций
docker-compose exec api python manage.py makemigrations
```

### Пересборка после изменений
```bash
docker-compose build --no-cache
docker-compose up -d
```

## 🔧 Разработка

### Локальная разработка (без Docker)

1. **Backend:**
```bash
cd API
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

2. **Frontend:**
```bash
cd frontend
npm install
npm start
```

### Переменные окружения

Основные переменные для `.env`:
- `SECRET_KEY` - секретный ключ Django
- `YANDEX_OAUTH2_KEY` - Client ID Яндекс OAuth
- `YANDEX_OAUTH2_SECRET` - Client Secret Яндекс OAuth
- `TEACHER_REGISTRATION_KEY` - ключ для регистрации преподавателей
- `DB_*` - настройки базы данных PostgreSQL

## 📝 API Endpoints

### Авторизация
- `GET /auth/login/yandex-oauth2/` - Авторизация через Яндекс
- `GET /api/auth/users/me/` - Информация о текущем пользователе

### Проекты
- `GET /api/projects/projects/` - Список проектов
- `POST /api/projects/projects/` - Создать проект
- `GET /api/projects/projects/{id}/` - Детали проекта
- `PATCH /api/projects/projects/{id}/` - Обновить проект
- `POST /api/projects/projects/{id}/submit/` - Отправить на проверку

### Канбан-карточки
- `GET /api/projects/kanban-cards/?project={id}` - Карточки проекта
- `POST /api/projects/kanban-cards/` - Создать карточку
- `PATCH /api/projects/kanban-cards/{id}/move/` - Переместить карточку
- `GET /api/projects/kanban-cards/{id}/` - Детали карточки

### Этапы
- `GET /api/projects/stages/?project={id}` - Этапы проекта
- `POST /api/projects/stages/{id}/submit/` - Отправить этап на проверку

## 🐛 Решение проблем

### Ошибки миграций

Если возникают ошибки с миграциями:
```bash
docker-compose exec api python manage.py migrate --fake-initial
```

### Очистка базы данных

```bash
docker-compose down -v
docker-compose up --build
```

### Проблемы с фронтендом

```bash
docker-compose build frontend --no-cache
docker-compose up -d frontend
```

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Проект разработан для управления проектной деятельностью студентов ДВФУ.

---

## 🥚 Пасхалка

> ⚠️ **Disclaimer**: Этот проект не является серьезным творением автора, а был создан максимально лениво, потому что нужно было сдать по проектной деятельности. Если вы нашли этот проект полезным - отлично! Если нет - ну, что поделать, проектная деятельность же ¯\\_(ツ)_/¯
